<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.alibaba.csp.sentinel.dashboard.datasource.mapper.AppMapper">
  <resultMap id="BaseResultMap" type="com.alibaba.csp.sentinel.dashboard.datasource.entity.App">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="app" jdbcType="VARCHAR" property="app" />
  </resultMap>
  <sql id="Where_Where_Clause">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Update_By_Where_Where_Clause">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    <where>
      <foreach collection="example.oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    id, app
  </sql>
  <select id="selectByWhere" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.AppWhere" resultMap="BaseResultMap">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    select
    <if test="distinct">
      distinct
    </if>
    <include refid="Base_Column_List" />
    from app
    <if test="_parameter != null">
      <include refid="Where_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
    <if test="limitOffset >= 0">
       limit ${limitOffset} , ${limitSize}
    </if>
  </select>
  <select id="selectById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    select 
    <include refid="Base_Column_List" />
    from app
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteById" parameterType="java.lang.Integer">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    delete from app
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <delete id="deleteByWhere" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.AppWhere">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    delete from app
    <if test="_parameter != null">
      <include refid="Where_Where_Clause" />
    </if>
  </delete>
  <insert id="insert" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.App">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into app (app)
    values (#{app,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.App">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into app
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="app != null">
        app,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="app != null">
        #{app,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <select id="countByWhere" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.AppWhere" resultType="java.lang.Long">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    select count(*) from app
    <if test="_parameter != null">
      <include refid="Where_Where_Clause" />
    </if>
  </select>
  <update id="updateByWhereSelective" parameterType="map">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    update app
    <set>
      <if test="record.id != null">
        id = #{record.id,jdbcType=INTEGER},
      </if>
      <if test="record.app != null">
        app = #{record.app,jdbcType=VARCHAR},
      </if>
    </set>
    <if test="_parameter != null">
      <include refid="Update_By_Where_Where_Clause" />
    </if>
  </update>
  <update id="updateByWhere" parameterType="map">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    update app
    set id = #{record.id,jdbcType=INTEGER},
      app = #{record.app,jdbcType=VARCHAR}
    <if test="_parameter != null">
      <include refid="Update_By_Where_Where_Clause" />
    </if>
  </update>
  <update id="updateByIdSelective" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.App">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    update app
    <set>
      <if test="app != null">
        app = #{app,jdbcType=VARCHAR},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateById" parameterType="com.alibaba.csp.sentinel.dashboard.datasource.entity.App">
    <!-- 警告 - 该元素是自动生成的，请勿修改  -->
    update app
    set app = #{app,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
</mapper>